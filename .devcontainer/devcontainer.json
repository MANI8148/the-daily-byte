{
  "name": "bloggy — free Codespaces dev environment",
  "image": "mcr.microsoft.com/devcontainers/universal:2-linux",
  "postCreateCommand": "python3 -m venv .venv && .venv/bin/pip install -r worker/requirements.txt -r scripts/requirements.txt && cd site && npm install",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "esbenp.prettier-vscode", "bradlc.vscode-tailwindcss"]
    }
  }
}